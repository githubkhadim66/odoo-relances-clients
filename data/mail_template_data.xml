<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="mail_template_relance_1" model="mail.template">
            <field name="name">Relance client : premier rappel</field>
            <field name="model_id" ref="model_account_relance"/>
            <field name="subject">Rappel : facture {{ object.move_id.name }} échue</field>
            <field name="email_from">{{ (object.user_id.email_formatted or user.email_formatted) }}</field>
            <field name="partner_to">{{ object.partner_id.id }}</field>
            <field name="body_html" type="html">
                <div style="margin:0px;padding:0px;font-size:14px;">
                    <p>Bonjour <t t-out="object.partner_id.name or ''"/>,</p>
                    <p>
                        Sauf erreur de notre part, la facture
                        <strong t-out="object.move_id.name or ''"/>
                        d'un montant restant dû de
                        <strong t-out="format_amount(object.amount_residual, object.currency_id) or ''"/>
                        est arrivée à échéance le
                        <t t-out="format_date(object.date_due) or ''"/>.
                    </p>
                    <p>
                        Si le règlement a été effectué entre-temps, merci de ne pas tenir compte de ce message.
                        Dans le cas contraire, nous vous remercions de bien vouloir procéder au paiement.
                    </p>
                    <p>Cordialement,<br/><t t-out="object.user_id.name or ''"/></p>
                </div>
            </field>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="auto_delete" eval="False"/>
        </record>

        <record id="mail_template_relance_2" model="mail.template">
            <field name="name">Relance client : mise en demeure</field>
            <field name="model_id" ref="model_account_relance"/>
            <field name="subject">Facture {{ object.move_id.name }} : relance formelle</field>
            <field name="email_from">{{ (object.user_id.email_formatted or user.email_formatted) }}</field>
            <field name="partner_to">{{ object.partner_id.id }}</field>
            <field name="body_html" type="html">
                <div style="margin:0px;padding:0px;font-size:14px;">
                    <p>Bonjour <t t-out="object.partner_id.name or ''"/>,</p>
                    <p>
                        Malgré nos précédents rappels, la facture
                        <strong t-out="object.move_id.name or ''"/>
                        reste impayée à hauteur de
                        <strong t-out="format_amount(object.amount_residual, object.currency_id) or ''"/>,
                        soit <t t-out="object.days_overdue or 0"/> jours de retard.
                    </p>
                    <p>
                        Nous vous remercions de régulariser cette situation dans les meilleurs délais
                        ou de nous contacter pour convenir d'un échéancier.
                    </p>
                    <p>Cordialement,<br/><t t-out="object.user_id.name or ''"/></p>
                </div>
            </field>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="auto_delete" eval="False"/>
        </record>

    </data>
</odoo>
